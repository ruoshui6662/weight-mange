# 项目协作与进度记录规则

本文件适用于整个仓库。任何开发者或自动化代理在修改代码、数据库、接口、部署配置或规划文档前，都必须先阅读：

1. `plan/nutrition_tracker_tech_manual_2026-09-08/README.md`
2. `plan/nutrition_tracker_tech_manual_2026-09-08/DEVELOPMENT_PROGRESS.md`
3. 当前任务对应的产品/技术规范

## 进度单一事实源

`DEVELOPMENT_PROGRESS.md` 是项目状态的单一事实源。聊天记录、提交信息和个人笔记不能替代它。

每个任务必须拥有稳定 ID，格式为 `M<里程碑>-<三位序号>`；文档治理任务使用 `DOC-<三位序号>`。

允许的状态只有：

- `PLANNED`：已定义，尚未开始；
- `IN_PROGRESS`：当前正在实施；
- `BLOCKED`：存在明确阻塞，必须记录原因和解除条件；
- `DONE`：验收命令已运行且证据已记录；
- `DESCOPED`：经决策移出当前范围，必须链接决策记录。

## 强制更新时间点

开始任务前：

1. 将任务状态改为 `IN_PROGRESS`；
2. 更新“当前焦点”和“下一步”；
3. 在活动日志追加开始时间、操作者和计划验收命令。

任务进行中：

- 出现阻塞立即改为 `BLOCKED`，写明影响、已尝试内容、解除条件；
- 需求或接口发生变化，先记录决策，再修改实现；
- 新发现但不属于当前任务的问题，登记到问题队列，不顺手扩大范围。

完成任务前：

1. 运行该任务定义的完整验收命令；
2. 记录命令、退出码和关键结果；
3. 更新相关规范或决策记录；
4. 只有证据通过后才能标记 `DONE`；
5. 更新里程碑统计、当前焦点、下一步和交接摘要。

所有时间使用 ISO 8601，默认项目时区为 `Asia/Shanghai`，例如 `2026-09-09 06:30 +08:00`。

## 文档与实现一致性

- 产品范围冲突时，以 `PRODUCT_SPEC.md` 和已接受的决策记录为准。
- API、数据库或计算行为变更，必须在同一任务中同步对应规范。
- 数据库迁移、备份恢复、营养计算和 AI 写入边界属于高风险区域，未经测试证据不得宣称完成。
- 禁止在文档、日志、fixtures 或提交中写入真实密码、Cookie、API Key、完整 Authorization 或含隐私的原始提示词。
- 当前目录尚未初始化 Git；初始化版本控制属于独立任务，不能在无明确任务记录时顺带执行。

